import PropTypes from 'prop-types'
const Blog = () => {
    return (
        <div>
            
        </div>
    );
};

export default Blog;



Blog.PropTypes = {
    blog : PropTypes.object.isRequired
}